<nav class="navbar navbar-inverse mb-4">
  <div class="container-fluid">
    <div class="navbar-header">
        <a class="navbar-brand" href="#">Book Store</a>
    </div>
    <div class="nav navbar-right">
      <li class="nav-item dropdown">
        <div class="dropdown-content">
        <% if current_user %>
            <% if current_user.seller? %> <li><%= button_to "Add new book", new_user_registration_path , :class => 'btn navbar-btn btn-primary mx-2' %></li><% end %>
            <% if current_user.admin? %> <li><%= button_to "Edit users", new_user_session_path, :class=> 'btn navbar-btn btn-secondary mx-2' %></li><% end %>
        <% else %>
            <li><%= link_to "Sign in", new_user_registration_path ,  :class => 'btn navbar-btn btn-primary mx-2' %></li>
            <li><%= button_to "Log in", new_user_session_path, :class=> 'btn navbar-btn btn-secondary mx-2' %></li>
        <% end %>
        </div>
      </li>
    </div>
    </div>

  </div>
  </nav>

